{
  "dependencies": {
    "gh-pages": "^6.1.1",
    "parcel": "^2.12.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel serve -d src/index.html",
    "build": "parcel build --dist-dir build --public-url /lol/ src/index.html",
    "deploy": "gh-pages -d build"
  }
}
